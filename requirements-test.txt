# Testing dependencies for the Auto-Paraphrasing System

# Core testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0  # For parallel test execution
pytest-mock>=3.10.0

# Test utilities
factory-boy>=3.2.0  # For creating test data
freezegun>=1.2.0    # For mocking datetime
responses>=0.23.0   # For mocking HTTP requests

# Performance testing
pytest-benchmark>=4.0.0
memory-profiler>=0.60.0
psutil>=5.9.0

# Test reporting
pytest-html>=3.1.0
pytest-json-report>=1.5.0

# Code quality
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0

# Development tools
httpx>=0.24.0      # For async HTTP testing
faker>=18.0.0      # For generating fake test data

# Dependencies already in main requirements that are needed for tests
fastapi>=0.100.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
uvicorn>=0.22.0
